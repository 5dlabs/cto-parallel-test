# JWT Secret Key
# IMPORTANT: Change this to a secure random value in production
# Generate a secure key with: openssl rand -base64 32
JWT_SECRET=development_secret_key_change_in_production_minimum_32_characters
