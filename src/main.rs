// Allow unused since this is a library module being used by other tasks
#![allow(dead_code, unused_imports)]

mod auth;

fn main() {
    // Placeholder main function
    // Schema and migrations are defined in schema.rs and migrations/
    // Authentication module is available for import by other tasks
}
