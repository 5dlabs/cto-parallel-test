pub mod config;
pub mod models;
pub mod schema;

#[cfg(test)]
mod tests {
    #[test]
    fn test_module_exports() {
        // This test verifies that the modules compile and are properly exported
        // No runtime assertions needed since compilation success is the test
    }
}
