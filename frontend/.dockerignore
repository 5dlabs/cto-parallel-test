# Prevent large or sensitive files from being sent to Docker build context
# Secrets and local environment
.env
.env.*
!.env.example

# Node modules and build artifacts
node_modules
dist
build
out

# VCS and CI/CD metadata
.git
.gitignore
.github

# Local security scan artifacts
../security
security
gitleaks*.json

# OS files
.DS_Store
Thumbs.db
