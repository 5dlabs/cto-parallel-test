/**
 * App.js - Route Structure Documentation
 * 
 * This file documents the routing structure of our Next.js 15 application.
 * Note: Next.js 15 uses the App Router (app directory) for routing.
 * 
 * Available Routes:
 * - / (HomePage) - Landing page with hero section and features
 * - /products (ProductList) - Grid view of all products
 * - /products/[id] (ProductDetail) - Individual product details page
 * - /cart (Cart) - Shopping cart page
 * - /login (Login) - User authentication page
 * - /register (Register) - New user registration page
 * 
 * Components Used:
 * - Header: Navigation bar with cart badge (appears on all pages)
 * - Footer: Simple footer (appears on all pages)
 * 
 * shadcn/ui Components Available:
 * - button, card, badge, input, form, label
 * - navigation-menu, select, textarea, checkbox, radio-group
 * 
 * All components are styled with Tailwind CSS and follow shadcn/ui design system.
 * 
 * To run the application:
 * cd frontend
 * npm install (or pnpm install)
 * npm run dev (or pnpm run dev)
 * 
 * Navigate to http://localhost:3000 to view the application.
 */

// This is a documentation file for the routing structure.
// The actual routing is handled by Next.js App Router in the src/app directory.
