# JWT Configuration
# For production, use a secure random string of at least 32 characters
JWT_SECRET=your_secure_random_secret_key_minimum_32_characters_long_change_in_production

# Database Configuration (for future use)
DATABASE_URL=postgresql://user:password@localhost/database_name
