# Copy to .env and set real values. Never commit .env.
# Use a real password when running locally; placeholder is redacted to avoid
# accidental credential patterns in the template.
DATABASE_URL=postgres://postgres:REDACTED@localhost:5432/ecommerce_db
# Optional pool tuning
# DB_POOL_MAX_SIZE=15
# DB_POOL_MIN_IDLE=5
# DB_POOL_CONNECTION_TIMEOUT_SECS=30
