# JWT_SECRET=<set_a_strong_random_secret_min_32_chars>
# Optional: customize minimum secret length (cannot be <32)
# JWT_SECRET_MIN_LEN=48
# Optional: token TTL in seconds (default 86400 = 24h, max 24 hours)
# JWT_TTL_SECS=86400
# Optional: issuer and audience to embed and validate
# JWT_ISSUER=your-issuer
# JWT_AUDIENCE=your-audience
## Development only: allow random in-process fallback key when JWT_SECRET is not set
# JWT_ALLOW_DEV_FALLBACK=0

## Argon2 tuning (bounded for safety)
# ARGON2_M_COST_KIB=65536   # memory (KiB), default 64 MiB, bounds: 8192–1048576
# ARGON2_T_COST=3           # iterations, default 3, bounds: 1–10
# ARGON2_P_COST=1           # parallelism, default 1, bounds: 1–8
