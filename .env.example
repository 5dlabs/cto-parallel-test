# Copy to .env (do not commit real secrets)
JWT_SECRET=change_me_to_a_long_random_value_at_least_32_bytes
# Optional: override default token lifetime
# JWT_EXP_HOURS=24
# Optional: set validation leeway (seconds), max 300, default 60
# JWT_LEEWAY_SECS=60
# Optional: enforce issuer/audience checks (recommended)
# JWT_ISSUER=your-issuer
# JWT_AUDIENCE=your-audience
