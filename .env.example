# JWT Secret Key
# IMPORTANT: In production, set this to a secure random string (minimum 32 characters)
# Generate with: openssl rand -base64 32
JWT_SECRET=your-secret-here-generate-with-openssl-rand
