# Copy to .env and set real values. Never commit .env.
DATABASE_URL=
# Format: <scheme>://<username>:<password>@<host>:<port>/<database>
# For PostgreSQL, use the "postgresql" scheme and set strong credentials.
# Optional pool tuning
# DB_POOL_MAX_SIZE=15
# DB_POOL_MIN_IDLE=5
# DB_POOL_CONNECTION_TIMEOUT_SECS=30
