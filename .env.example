# JWT Secret Key (minimum 32 characters recommended)
# IMPORTANT: Change this value in production and keep it secret
# Example: Generate with: openssl rand -base64 32
JWT_SECRET=CHANGE_THIS_IN_PRODUCTION_USE_OPENSSL_RAND_BASE64_32
