[package]
name = "cto_parallel_test"
version = "0.1.0"
edition = "2024"

[dependencies]
rust_decimal = { version = "1.30", features = ["serde"] }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
