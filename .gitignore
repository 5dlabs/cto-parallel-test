/target
Cargo.lock

# Environment variables - never commit
.env
.env.*
!.env.example

# Hook files - never commit
hooks/
.hooks/
**/hooks/

# Security and audit reports - do not commit
# Ignore gitleaks reports (prevent leaking into history)
gitleaks-report.json
gitleaks*.json
audit.json
